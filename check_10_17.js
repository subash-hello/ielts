const mongoose = require('mongoose'); const TestContent = require('./src/models/TestContent'); async function run() { await mongoose.connect('mongodb://127.0.0.1:27017/ielts-ai'); const tests = await TestContent.find({ title: /Cambridge IELTS (1[0-7])/ }); console.log('Found ' + tests.length + ' tests'); if (tests.length > 0) { console.log('Test Title:', tests[0].title); console.log('Sample questions:', JSON.stringify(tests[0].content.parts[0].questions.slice(0,2), null, 2)); } process.exit(0); } run();
